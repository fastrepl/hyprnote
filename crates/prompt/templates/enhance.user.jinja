My profile:

```
{{ config_profile | json_encode() }}
```

Raw Note:

```
{{ final_editor }}
```

Meeting Transcript:

```
{{ render_conversation(transcripts=transcripts, diarizations=diarizations) }}
```
