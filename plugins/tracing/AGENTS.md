- Logs are stored in `$XDG_STATE_HOME/hyprnote/logs/` (Linux) or `$DATA_DIR/hyprnote/logs/` (macOS/Windows).
- A `README.md` is written to the log directory at runtime explaining the log format and rotation policy.
