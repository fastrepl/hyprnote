[package]
name = "vad"
version = "0.1.0"
edition = "2021"

[dependencies]
kalosm-sound = { workspace = true, default-features = false }
tokio = { workspace = true, features = ["rt-multi-thread"] }

silero = { git = "https://github.com/emotechlab/silero-rs", rev = "99dc5ea" }
